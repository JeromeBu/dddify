{
  "name": "dddify",
  "type": "module",
  "version": "0.0.2",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "rm -rf ./dist && tsc",
    "publish": "npm publish"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "config": "workspace:*",
    "typescript": "^4.5.3"
  }
}
